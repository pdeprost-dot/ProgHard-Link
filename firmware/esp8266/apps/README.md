# ESPway applications

This directory is reserved for product-specific applications built with ESPway,
for example `BoilerController`, `RelayBoard` or `CameraController`.

Official teaching and demonstration sketches belong under
`libraries/ESPway/examples` so Arduino IDE exposes them with the ESPway library.
